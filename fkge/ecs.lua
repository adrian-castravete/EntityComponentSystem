local function component()
end

local function system()
end

local function entity()
end

local function scene()
end

return {
  scene = scene,
  component = component,
  entity = entity,
  system = system,
  c = component,
  e = entity,
  s = system,
}